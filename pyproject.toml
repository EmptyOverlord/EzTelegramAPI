[project]
name = "eztelegramapi"
version = "0.1.8"
description = "Simple Telegram helper"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["requests"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"